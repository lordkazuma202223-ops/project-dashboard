# Ngrok Tunnel Configuration
# Copy this to your project's .env.local for Vercel deployment

NEXT_PUBLIC_OPENCLAW_GATEWAY_URL=https://unregularized-suboesophageal-ardith.ngrok-free.dev
GATEWAY_TOKEN=your_gateway_token_here

# Note: ngrok URLs change when you restart the tunnel.
# To keep a persistent URL, create a free ngrok account and
# configure a custom subdomain.
